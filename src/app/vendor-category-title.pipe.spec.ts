import { VendorCategoryTitlePipe } from './vendor-category-title.pipe';

describe('VendorCategoryTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new VendorCategoryTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
