import { VendorCategoryAvatarPipe } from './vendor-category-avatar.pipe';

describe('VendorCategoryAvatarPipe', () => {
  it('create an instance', () => {
    const pipe = new VendorCategoryAvatarPipe();
    expect(pipe).toBeTruthy();
  });
});
